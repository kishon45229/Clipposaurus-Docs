import DocsPage from "../page";

/**
 * Dynamic Docs Page Component
 * Handles all documentation routes by reusing the root DocsPage.
 * @returns SERVER COMPONENT
 */
export default function DynamicDocsPage() {
  return <DocsPage />;
}